import { InitAction } from "typings/actions";
// import  from "services/giftcard";

const initActionGiftcard: InitAction = async (dispatch, { search }) => {
  // await SearchService.onLoadSearchPage(dispatch, search);
};

export default initActionGiftcard;
