import { Currency } from "typings/currency";

export type Section1Props = {
  giftimages: string[];
};

export type Section2Props = {
  countryData: any;
  productData: any;
  mobile: boolean;
  currency: Currency;
};
