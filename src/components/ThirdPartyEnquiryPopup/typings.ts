export type Props = {
  quantity: number;
};

export type Fields = "name" | "contactNo" | "email" | "quantity" | "query";

export type StateOptions = {
  value: string;
  label: string;
  id: number;
  nameAscii: string;
};
